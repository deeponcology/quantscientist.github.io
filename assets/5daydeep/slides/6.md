# Curriculum

```bash
├── Day 1: Practical machine learning with Python and sk-learn pipelines
│   └──
├── Day 2: Neural networks using the GPU, PyCUDA, PyTorch and Matlab
│   └──
├── Day 3: Applied Deep Learning in Python
│   └──
├── Day 4: Convolutional Neural Networks using Keras
│   └──
├── Day 5: Applied Deep Reinforcement Learning in Python
│   └──
├── .gitignore (its a `joke` ...)

```

```python
Running:XGBClassifier(base_score=0.5, colsample_bylevel=1, colsample_bytree=0.5,
       gamma=0.017, learning_rate=0.15, max_delta_step=0, max_depth=12,
       min_child_weight=3, missing=None, n_estimators=2000, nthread=-1,
       objective='binary:logistic', reg_alpha=0, reg_lambda=1,
       scale_pos_weight=1, seed=0, silent=1, subsample=0.9)shape:(6833, 576)
```
